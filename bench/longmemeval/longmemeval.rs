//! LongMemEval benchmark harness for Elephant.

#[path = "../common/mod.rs"]
mod common;
mod dataset;
mod ingest;

fn main() {
    eprintln!("longmemeval-bench: not yet implemented (Phase 3)");
    std::process::exit(1);
}
