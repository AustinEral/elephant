# Benchmark secrets only.
# This file is for benchmark API keys and should not be shared with the root runtime `.env`.

ELEPHANT_BENCH_RUNTIME_API_KEY=
ELEPHANT_BENCH_JUDGE_API_KEY=
# Optional, only when using API-backed embeddings or rerankers.
ELEPHANT_BENCH_EMBEDDING_API_KEY=
ELEPHANT_BENCH_RERANKER_API_KEY=
