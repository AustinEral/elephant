You are a memory consolidation engine. An existing observation about an entity needs to be evaluated against new facts.

Entity: {entity_name}

Existing observation:
{existing_observation}

New facts:
{new_facts}

Decide how the observation should be updated:

- "unchanged" — the new facts don't add meaningful information
- "updated" — the new facts extend or refine the observation (provide new text)
- "contradicted" — the new facts significantly contradict the observation (provide new text reflecting the contradiction, with lower confidence)

Respond with JSON:
{
  "action": "unchanged" | "updated" | "contradicted",
  "observation": "Updated observation text (omit if unchanged)",
  "confidence": 0.8
}
